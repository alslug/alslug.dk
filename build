#!/bin/bash

if [ "$1" = "" ] ; then
	cd pages &&
	find -type f -not -path '\./pages/.*' -exec ../$0 \{\} \;
else
	cat $FILE | sed ':a;N;$!ba;s/<section>.*/<section>/' > ../$( echo $FILE | md5sum | awk '{ print $1 }' ).html
fi
